{{#if isPerson}}
    <div class="presence-status {{presenceClass}}"></div>
{{else}}
    <i class="fa fa-group"></i>
{{/if}}
<span class="subject">{{room.subject}}</span>
<button class="close-button" {{action closeInteraction room}}>
    {{material-icon "clear"}}
</button>